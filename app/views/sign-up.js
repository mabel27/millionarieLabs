// app/views/sign-up.js

import Ember from 'ember';

export default Ember.View.extend({
  classNames: ['sign-up-page-top'],
  didInsertElement: function() {
    // window.scrollTo(0, 0);
    window.scrollTo(0, 0);
  }
});